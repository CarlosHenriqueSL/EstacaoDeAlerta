
cmake_minimum_required(VERSION 3.13)

set(CMAKE_C_STANDARD 11)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

# == DO NOT EDIT THE FOLLOWING LINES for the Raspberry Pi Pico VS Code Extension to work ==
if(WIN32)
    set(USERHOME $ENV{USERPROFILE})
else()
    set(USERHOME $ENV{HOME})
endif()
set(sdkVersion 2.1.0)
set(toolchainVersion 13_3_Rel1)
set(picotoolVersion 2.1.0)
set(picoVscode ${USERHOME}/.pico-sdk/cmake/pico-vscode.cmake)
if (EXISTS ${picoVscode})
    include(${picoVscode})
endif()
# ====================================================================================
# Generated Cmake Pico project file

set(PICO_BOARD pico_w CACHE STRING "Board type")

# Pull in Raspberry Pi Pico SDK (must be before project)
include(pico_sdk_import.cmake)
set(FREERTOS_KERNEL_PATH "/home/carlos2/FreeRTOS-Kernel")
include(${FREERTOS_KERNEL_PATH}/portable/ThirdParty/GCC/RP2040/FreeRTOS_Kernel_import.cmake)

project(EstacaoDeAlerta C CXX ASM)

# Initialise the Raspberry Pi Pico SDK
pico_sdk_init()

# Add executable. Default name is the project name, version 0.1

add_executable(EstacaoDeAlerta EstacaoDeAlerta.c lib/ssd1306.c lib/matriz.c)

pico_set_program_name(EstacaoDeAlerta "EstacaoDeAlerta")
pico_set_program_version(EstacaoDeAlerta "0.1")

# Generate PIO header
pico_generate_pio_header(EstacaoDeAlerta ${CMAKE_CURRENT_LIST_DIR}/blink.pio)

# Modify the below lines to enable/disable output over UART/USB
pico_enable_stdio_uart(EstacaoDeAlerta 1)
pico_enable_stdio_usb(EstacaoDeAlerta 1)

# Add the standard library to the build
target_link_libraries(EstacaoDeAlerta
        pico_stdlib)

include_directories(${CMAKE_SOURCE_DIR}/lib)

# Add the standard include files to the build
target_include_directories(EstacaoDeAlerta PRIVATE
        ${CMAKE_CURRENT_LIST_DIR}
)

# Add any user requested libraries
target_link_libraries(EstacaoDeAlerta 
        hardware_adc
        hardware_i2c
        hardware_pio
        hardware_pwm
        FreeRTOS-Kernel 
        FreeRTOS-Kernel-Heap4
        )

pico_add_extra_outputs(EstacaoDeAlerta)
